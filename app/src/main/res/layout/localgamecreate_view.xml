<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    app:cardCornerRadius="@dimen/main_radius"
    android:padding="@dimen/button_margin">

    <TextView
        android:id="@+id/dialogTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/new_game_name"
        android:textSize="18sp"/>

    <EditText
        android:id="@+id/editGameName"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:hint="@string/game_name"
        android:text="@string/game"
        android:inputType="text"
        android:layout_marginTop="16dp" />

    <ToggleButton
        android:id="@+id/sideToogleButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:checked="true"
        android:textOn="@string/play_white"
        android:textOff="@string/play_black"
        android:background="@drawable/toogle_side_shape"
        android:textColor="@drawable/toogle_text_selector" />

</LinearLayout>
