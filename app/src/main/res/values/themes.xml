<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ChessGPT" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/mainColor</item>
        <item name="colorPrimary">@color/mainColor</item>
        <item name="materialButtonStyle">@style/button_style</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="colorAccent">@color/textColor</item>
        <item name="android:textSize">@dimen/button_text_size</item>
    </style>

    <style name="Theme.ChessGPT" parent="Base.Theme.ChessGPT" />
</resources>